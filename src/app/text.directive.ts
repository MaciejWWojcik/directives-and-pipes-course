import { Directive, ElementRef } from '@angular/core';

@Directive({
  selector: '...' // TODO fill the selector properly
})
export class TextDirective {
  // TODO implement a directive for exposing the text input value
}
